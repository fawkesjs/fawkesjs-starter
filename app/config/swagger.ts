module.exports = {
  info: {
    description: "Default with admin account 'admin@localhost.com' \
    password 'admin'. Login with /api/account/login will return access_token \
    in object id. The access_token can be set above swagger Authorize button. \
    Note: If you login with cookie true, you do not need to set the access_token \
    in swagger Authorize button",
    title: "FawkesJs Starter",
    version: "0.3.0",
  },
};
